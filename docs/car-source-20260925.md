# 2026-09-25 小车源码同步记录

## 来源与范围

通过 SSH 从主机 `smartcar-desktop`、用户 `smartcar` 的 `/home/smartcar` 读取以下三个目录：`newcar_ws`、`lane_follow_20260923`、`lane_follow_20260923_v2`。源码保持原内容；本次不重构历史版本，也不修改设备配置或启动驾驶程序。

纳入 271 个条目：263 个普通文件、8 个符号链接。普通文件共 1,942,004 字节。除源码外保留配置、ROS 消息与启动文件、已有图片资源、第三方许可证和包内文档。

[文件清单](car-source-20260925-manifest.json)记录读取时间、相对路径、SHA-256、Git 文件模式及符号链接目标。清单中的路径相对车上 `/home/smartcar`，在仓库中对应 `vehicle/` 下的同名路径。每个下载文件均已与车上计算的 SHA-256 对比；符号链接的 SHA-256 对应其目标路径文本。

这是一份逐文件验证的源码快照，不是文件系统级原子快照，也不包括车上整个用户目录或系统安装的依赖。

## 排除内容

清单中的 `excluded` 记录 40 个被排除的文件或目录条目；目录中的所有内容一起排除。主要包括：

- 嵌套 `.git` 历史和编辑器状态目录；代码作为普通源码导入，不创建子模块。
- ROS `build/`、`devel/` 等编译输出。
- `runs/`、缓存、Python 字节码、日志和打包归档。
- 凭据类文件、模型权重、大型数据和系统镜像不属于本次源码同步范围。

SSH 登录密码、电脑连接配置和本地下载工具未上传。对全部待上传文件进行了凭据模式检查，未发现登录密码、私钥、GitHub 令牌或带认证信息的 URL；关键词命中为许可证中的授权描述和串口库的字符串分词示例。该检查不等于完整安全审计。

## 验证

在车上原 Python 环境执行已有的纯算法单元测试，并用 `-B` 避免新增字节码：

```bash
cd /home/smartcar/lane_follow_20260923
python -B -m unittest -v test_lane
# 3 tests: OK

cd /home/smartcar/lane_follow_20260923_v2
python -B -m unittest -v test_lane
# 6 tests: OK
```

覆盖循迹方向/限幅、无效边线拒绝、停车条件，以及 v2 的阻尼控制、无效时间输入和单帧跳变处理。本次没有启动 ROS 驱动或运动程序，没有进行实车行驶，也没有重新构建全部历史 ROS 包；测试结果不能证明所有历史业务程序可用。

## 已知问题与后续

- 车上已有的 `newcar_ws/znxc/config/teleop/src/smartcar/scripts/0.jpg` 是不完整图片，严格解码报告 truncated。按原始字节保留，未修复或替换。
- 旧代码仍包含历史路径、重复工作空间和不同版本的接口约定，后续整理应单独提交并验证。
- 原有脚本可执行位和八个 Catkin 链接在 Git 中保留。`vehicle/**` 禁用换行转换，以便从 Git 对照原文件校验值。
- 本次上传到 `snapshot/car-20260925` 分支，通过 PR 审阅后再并入 `main`。
